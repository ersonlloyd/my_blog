- provide(:title, @user.name)

.contianer.col-md-8
  .user_info
    .container.col-md-2
      .profile-img= gravatar_for @user
    .container-username.col-md-5
      %h1.user-name= @user.name
      %h2.user-email= @user.email
    .container
      .center.jumbotron
